import { Metadata } from "next";

// template
import AddAdvertisementPage from "@/template/AddAdvertisementPage";

// export const metaData: Metadata = {
//   title: "Real Estate | Add Advertisement",
//   description: "Add Advertisement page for publish it.",
// };

const AddAdvertisement = () => {
  return <AddAdvertisementPage />;
};

export default AddAdvertisement;
